// Basic imports
import React from "react";
import styles from "./Header.module.css";

// Rendering the children inside the background div
const Header = () => {
  return <div className={styles.header}>MiniURLs</div>;
};
export default Header;
